# Generador de documentación RITE — dependencias

# --- Generación de documentos ---
reportlab>=4.0              # PDF (Guía JE6). El .docx no requiere librerías externas.

# --- Conexión a datos ---
psycopg[binary]>=3.1        # Supabase (Postgres)

# --- API / despliegue ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Subida a Google Drive ---
google-api-python-client>=2.0
google-auth>=2.0
