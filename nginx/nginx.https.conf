events { worker_connections 1024; }

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;

    # Subida de documentación del expediente (incl. vídeos del recorrido de la
    # vivienda). Debe ir alineado con el límite de multer en el backend (120MB).
    client_max_body_size 120m;

    # HTTP → HTTPS redirect
    server {
        listen 80;
        server_name app.brokergy.es;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # HTTPS
    server {
        listen 443 ssl http2;
        server_name app.brokergy.es;

        ssl_certificate /etc/letsencrypt/live/app.brokergy.es/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/app.brokergy.es/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;

        # API proxy
        location /api/ {
            proxy_pass http://backend:3000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_read_timeout 120s;
            proxy_connect_timeout 60s;
            proxy_send_timeout 120s;
        }

        # Health check
        location /health {
            proxy_pass http://backend:3000;
            proxy_set_header Host $host;
        }

        # MCP Server — Brokergy Expedientes (Claude AI)
        # SSE requiere: sin buffer, sin caché, conexión keep-alive larga
        location /mcp/ {
            proxy_pass http://mcp:3001/;
            proxy_http_version 1.1;
            proxy_set_header Connection '';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header x-api-key $http_x_api_key;
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 3600s;
        }

        # SPA
        root /usr/share/nginx/html;
        index index.html;
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
